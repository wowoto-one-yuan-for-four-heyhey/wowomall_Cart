<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xmu.wowomall.cart.mapper.CartMapper">

<select id="getCartItems" parameterType="java.util.List" resultType="WowoCartItem">
    select *
    from cart_item
    where user_id = #{userId}
</select>

<select id="getCartItemsByUserIdAndProductId" parameterType="java.util.List" resultType="WowoCartItem">
    select *
    from cart_item
    where user_id = #{userId} and product_id = #{productId}
</select>

</mapper>